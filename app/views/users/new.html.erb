<div id="clearance" class="sign-up">
  <h1><%= t(".title") %></h1>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <div class="submit-field">
      <%= form.submit class: "btn btn-primary" %>
      <%= link_to t(".sign_in"), sign_in_path, class: "btn btn-link" %>
    </div>
  <% end %>
</div>
